$(document).read(function() {
    DEFAULT_COOKIE_EXPIRE_TIME = 30;
    uname = '';
    session = '';
    uid = 0;
    currentVideo = null;
    listedVideos = null;
    sesssion = getCookie('seesion')
    uname = getCookie('username')

    $("#regbtn").on('click', function(e) {
    })
})

function setCookie(cname, cvalue, exmin) {
}

function getCookie(cname) {
}
